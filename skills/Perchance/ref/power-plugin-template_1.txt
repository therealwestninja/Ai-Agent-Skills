// Note that this is intended for use with me only.

// Do you want to create your own plugins off of this template? Sure!
// Credits are not mandatory, but it's highly appreciated!

// Feel free to comment for any suggestions or ideas
// by clicking on the comments button at the bottom/
// top of the page!

title
  Power Plugin Template

// uncomment this for the badges to work:
// temp = {import:power-plugin-template}
temps = {import:power-plugin-temps}

comm = {import:comments-plugin}
ico = {import:icon-plugin-v6}

starticon = puzzle-piece
endicon = puzzle-piece

$output(obj) =>
  // insert some code here
  
  // return `output`;
  
  // remove these lines below:
  switch (obj) {
    case "new": return `<span style\="background-color: #0D49CE; padding: 4px 8px; color: white; font-weight: bold; font-size: 12px; border-radius: 24px; margin-right: 4px">New!</span>`; break;
    case "buggy": return `<span style\="background-color: #FF7110; padding: 4px 8px; color: white; font-weight: bold; font-size: 12px; border-radius: 24px; margin-right: 4px">Experimental!</span>`; break;
    default: return `This is a template!`;
  }
  // (it's safe to replace the whole $output code here with yours, if you're developing outside of the plugin template)

