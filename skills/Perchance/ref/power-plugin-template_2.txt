{import:power-plugin-styler}
<h1><i class="fas fa-[starticon]" style="margin-right: 16px; font-size: 32px"></i>[title]<i class="fas fa-[endicon]" style="margin-left: 16px; font-size: 32px"></i></h1>
<div style="margin: 0 0 -16px; padding-top: 4px">[temps.credit(gName)]</div>
<div style="padding: 16px">
  <div style="overflow: auto">
    [temps.commandBar("top")]
  </div>
  <div class="main">
    <div class="info">
      <p>Insert some introductionary stuff here that explains what your plugin is all about, what your plugin exactly works, what this could be used for, etc. Be sure to use simple words that are understandable to most people abroad, especially to those who have minimal understanding in English.</p>
    </div>
    <p>Insert this code into the list panel:</p>
    <pre>theplugin = \{import:[generatorName]\}</pre>
    <p>Then write this code anywhere in the HTML panel:</p>
    <pre>\[theplugin()\]</pre>
    <p>You will get this:</p>
    <p style="text-align: center">[$output()]</p>
    <div class="info">
      <p>That above is a very simple demonstration of your plugin. First show how it was outputted with the above code, then show the result afterwards.</p>
    </div>
    <p>If you want the text to be bigger, you can set these options:</p>
    <pre>pluginOptions
  fontSize = 32px</pre>
    <p>and then put this into the HTML:</p>
    <pre>\[theplugin(pluginOptions)\]</pre>
    <p style="text-align: center; font-size: 32px">[$output()]</p>
    <br>
    <div class="info">
      <p>If your plugin has some available options that can be passed into parameters or list options, explain them one by one, write the available values or types of values if any, and explain how does each affects the functionality of your plugin.</p>
    </div>
    <p>Yes, this is a template for my plugins. I will create plugins using this template.</p>
    <p>You can insert a marker for new features: [$output("new")]</p>
    <p>You can insert a marker for buggy/experimental features: [$output("buggy")]</p>
    <p><b>Note:</b> The content below this paragraph is just a placeholder.</p>
    <div class="info">
      <p>Add warnings if necessary. Warnings are used to, well, warn users about unwanted things to happen in your plugin (e.g. experimental use, unwanted results).</p>
    </div>
    <div class="warning">
      <p>There is a much simpler way to reverse strings that works the same as the example below. By typing <code>\[\[..."Hello there!"\].reverse().join("")\]</code> you will get this: <b>!ereht olleH</b>.</p>
      <p>If you want to do the same above with an item from a list, type <code>\[\[...listname.evaluateItem\].reverse().join("")\]</code> instead.</p>
    </div>
    <p>This is a simple tutorial of how to reverse strings in JavaScript. Insert this code into the HTML panel:</p>
    <pre>
&lt;script>
  function revString(x) \{
    let y = "";
    for (let i = 0; i &lt; x.length; i++) \{
      y = x.substr(i, 1) + y;
    \}
    return y;
  \}
&lt;/script></pre>
    <p>And then this:</p>
    <pre>
&lt;p>&lt;textarea id="a">&lt;/textarea>&lt;/p>
&lt;p>&lt;button onclick="b.value = revString(a.value)">Reverse&lt;/button>&lt;/p>
&lt;p>&lt;textarea id="b">&lt;/textarea>&lt;/p></pre>
    <p>It should look (and function) like this:</p>
    <p style="text-align: center;"><textarea id="a"></textarea></p>
    <p style="text-align: center;"><button onclick="b.value = revString(a.value)">Reverse</button></p>
    <p style="text-align: center;"><textarea id="b"></textarea></p>
    <p><b>Notes</b></p>
    <ul>
      <li>You can place the <code>&lt;script></code> element with the contents (scripts) anywhere in the HTML panel, even inside of another element.</li>
      <li>You can remove all the info bars in a very quick way by pressing Ctrl+H in the HTML panel to open up Find and Replace, finding for this exact regex: <code>/&lt;div class="info">.*\\n.*\\n.*&lt;\/div>\\n\\s\\s\\s\\s/g</code> and replacing it with nothing into the editor (make sure to click "All" to remove all occurences).</li>
      <li>You might want to check out another <a href="plugin-template">plugin template</a> that allows you to make plugins and stuff like this.</li>
      <li>You can make a suggestion for upcoming plugins and features in the comments (click on the "comments" button at the top or bottom of the page).</li>
      <li>To start making your plugin from scratch out of this plugin page, you can change or remove anything in the HTML as you want and tinker with the list code on the left and replace the aforementioned section with your plugin's code.</li>
      <li>[temps.suggestionfeedback]</li>
      <li>[temps.maybefixed]</li>
    </ul>
  </div>
  <div style="overflow: auto">
    [temps.commandBar("bottom")]
  </div>
</div>

[temps.comments(comm(temps.copt))]
[temps.credit(gName)]

<script>
  let gName = "power-plugin-template";
  
  // You can remove this by the way if you're going to revamp the content completely
  function revString(x) {
    let y = "";
    for (let i = 0; i < x.length; i++) {
      y = x.substr(i, 1) + y;
    }
    return y;
  }
</script>
